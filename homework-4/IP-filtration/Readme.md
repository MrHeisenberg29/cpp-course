Для проверки корректности сортировки я запустил через git bash следующую команду:

cat ip_filter.tsv | ./main.exe | tr -d '\r' | md5sum

Утилита tr вырезает из '\r\n' часть '\r', и тогда хэш считается аналогично выполнению на Linux.